<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        
<mapper namespace="com.open.mis.modules.admin.loginSystem.userManagement.dao.UserManagementDao">
	
	<select id="getUsers" resultType="map">
		<choose>
			<when test="isTotal == 'yes' ">
				SELECT COUNT(1) AS TOTAL
			</when>
			<otherwise>
			SELECT * FROM (
				SELECT USER_ID,USER_CODE ,USER_NAME,ROWNUM AS FN 
			</otherwise>
		</choose>
				FROM SYS_USER  
				WHERE USER_CODE LIKE '%'||#{USER_CODE}||'%'  
				AND USER_NAME LIKE '%'||#{USER_NAME}||'%'
		
		<if test=" isTotal != 'yes'">	
				and ROWNUM <![CDATA[ <=  ]]>  #{end}
			)
			WHERE  FN > #{start}
		</if> 
		 		 
	</select>
	
	
</mapper>